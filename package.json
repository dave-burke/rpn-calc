{
  "name": "rpn-calc",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "infra"
  ],
  "scripts": {
    "check": "cd frontend && npx eslint . && npx prettier src/ && npx vitest --run && npm run build && cd ../infra && npm run lint"
  }
}
