{
  "name": "rpn-calc",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "infra"
  ],
  "scripts": {
    "check": "cd frontend && npm run lint && npx vitest --run && npm run build && ../infra && npm run lint"
  }
}
